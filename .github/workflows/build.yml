name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build single-file executable
        shell: powershell
        run: .\build.ps1

      - name: Verify output
        shell: powershell
        run: |
          if (-not (Test-Path .\dist\SteamUnlock.exe) -or
              (Get-Item .\dist\SteamUnlock.exe).Length -eq 0) {
            throw 'Build output is missing or empty.'
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: SteamUnlock-windows
          path: dist/SteamUnlock.exe
          if-no-files-found: error
